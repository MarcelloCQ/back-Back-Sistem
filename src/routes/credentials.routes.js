import { Router } from "express";
import { methods as credentialsControllers } from "../controllers/credentials.controller";

const router =  Router();

// router.get('/', credentialsControllers.getWorkers);
// router.get('/:numempleado', credentialsControllers.getWorker);
// router.post('/', credentialsControllers.insertWorker);
// router.delete('/:numempleado', credentialsControllers.deleteWorker);
// router.put('/:id', credentialsControllers.updateWorker);

export default router;